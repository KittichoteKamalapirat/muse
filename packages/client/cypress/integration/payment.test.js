describe("payment", () => {
  it("user can make payment via QR code", () => {
    cy.visit("/");
    // cy.visit(`${Cypress.env("clientUrl")}`);
    //visit the site
    //login
    //create an order
    //make a payment
  });
});
