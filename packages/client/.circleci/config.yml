version: 2.1
orbs:
  node: circleci/node@4.7
workflows:
  sample:
    jobs:
      - node/test:
          version: "16.10"
          pkg-manager: yarn
      - checkout
      - run: yarn install
      - run: yarn test
      - run: yarn build
      # - run: vercel --prod
