query postsByCreator($userId: String!) {
  postsByCreator(userId: $userId) {
    id
    title
    text
    points
    isPublished
    video {
      id
      url
    }
    image {
      id
      url
    }
  }
}
