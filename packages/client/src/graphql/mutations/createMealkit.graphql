mutation createMealkit(
  $input: MealkitInput!
  $postId: Int!
  $fileIds: [Int!]!
) {
  createMealkit(input: $input, postId: $postId, fileIds: $fileIds) {
    name
    items
    price
    portion
    id
  }
}
