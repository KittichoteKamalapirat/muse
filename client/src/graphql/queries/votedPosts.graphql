query votedPosts($limit: Int!, $cursor: String) {
  votedPosts(limit: $limit, cursor: $cursor) {
    posts {
      id
      title
      textSnippet
      videoUrl
      thumbnailUrl
    }
    hasMore
  }
}
