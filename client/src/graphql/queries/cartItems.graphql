query cartItems($status: OrderStatus!) {
  cartItems(status: $status) {
    id
    quantity
    userId
    mealkitId
    total
    user {
      username
    }
    mealkit {
      name
      images
      price
      portion
      post {
        id
        title
      }
    }
  }
}
