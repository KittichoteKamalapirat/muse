query creatorOrderItems($status: OrderStatus!) {
  creatorOrderItems(status: $status) {
    id
    quantity
    userId
    mealkitId
    mealkit {
      id
      name
      price
      images
    }
    orderId
    total
  }
}
