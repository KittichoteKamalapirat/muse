mutation createPaymentInfo($input: PaymentInfoInput!) {
  createPaymentInfo(input: $input) {
    errors {
      field
      message
    }
    paymentInfo {
      id
      userId
      bankCode
      bankAccount
    }
  }
}
